"""
Optional extra widgets built from the core pgwidgets primitives.

These are higher-level composite widgets (file browsers, dialogs, etc.)
that aren't part of the core widget set but ship with pgwidgets for
convenience.

Import individual modules as needed::

    from pgwidgets.extras.file_browser import FileBrowser
"""
